# Los archivos internos (scripts, CSS sin minificar, configuración) los cierra
# functions/_middleware.js y NO este archivo: _redirects no se aplica a rutas
# que ya existen como archivo, y esos archivos existen en el despliegue.

# URLs limpias: /reglamento sirve reglamento.html
/:splat.html   /:splat   301
